# Project conventions

- Branch names must not use the `codex/` prefix. Use a descriptive name such as `experimental-tab-completion`, unless the user specifies another name. This is an explicit user preference.
