# Deepseedian 0.7.0 验证记录

日期：2026-09-26。DSH 0.1.7-rc.2，macOS Obsidian 1.13.7。0.6.6 已发布，本次修复后递增为 0.7.0；用户授权真实文章补全验证和社区提交。

## 功能复审与修复

复查补全上下文、响应协议、取消与候选生命周期、调用额度，以及本次变更对聊天/记忆/目录路径的影响。新增校验：候选末尾重复后文已有的句末标点（包括带 Markdown 结束符的情况）时拒绝展示，不擅自修改候选或原文。提示词明确优先接完光标前最后一个短语，避免转而解释其他段落。

设置、命令及错误提示统一称为“手动补全”；保留默认关闭、付费供应商说明、取消/撤销、次数限制和事实错误边界。旧实验文档作为历史记录保留。

## 真实文章付费验证

用户明确授权后，从博客的 GitHub Actions、LR/SLR 文法、动态搜索框三篇现有文章各选取片段，共 8 个挖空位置。只在内存中构造缺口，原文件不作修改。请求经过生产上下文提取、真实 DSH 和官方付费 deepseek-flash，无工具、无应用层重试；原文参考答案不发送。片段和完整报告保存在 Git 忽略的 .runs，不发布文章正文。

| 阶段 | 结果 |
| --- | --- |
| 修复前 8 次 | 1 次错误；5 条可用；1 条偏离当前短语；1 条重复标点/Markdown 结束符 |
| 提示词调整后 8 次 | 8 次请求完成；最终规则复核为6条可用、1条被保护拒绝、1条语义不完整 |

后一轮暖请求 p50 为587ms，P95为869ms；样本量小，不代表长期真实接受率。保留失败：文法拓广的目的只补出“唯一”，拼接后语句不完整。搜索框候选擅自增加“清空”，同时重复已有句号，最终格式保护将其拒绝；保护不能证明模型本身已理解正确。首轮错误未因复测成功而从记录删除。

本地报告：`.runs/real-article-completion-067/report.json` 与 `final/report.json`。目录中的 067 是工作中临时编号，实际发行版本为 0.7.0。最终候选判定以独立评阅记录为准，其中标点校验在收到响应后补全，使用相同原始响应重新评估，不额外请求或挑选结果。

## 合成质量回归

调整提示词后运行20个固定样例，全部请求完成并报告用量：15条候选、1条格式拒绝、4次弃答。逐例评阅为14条可用、1条明确语义失败、1条保护拒绝、4次合理弃答。失败将召回率错误描述为“真实的负例里有多少被预测为正例”。不宣称可以可靠纠正前文或校验事实，不以HTTP成功作为质量通过。

报告 `.runs/live-completion-BR8B9L`，包含完整响应和逐例评阅。暖请求 p50为561ms，P95为979ms，仅表示本次串行小样本，不含冷启动。

## 原生体验与数据完整性

在用户 content 库安装0.7.0三个标准文件并重载，确认显示 Deepseedian、DSH rc.2 和无实验字样的补全命令。临时笔记使用真实文章片段副本：付费候选说明 PowerShell 执行策略；灰字期间磁盘未变，Tab插入，一次Cmd+Z并保存后逐字恢复。临时笔记归档至忽略目录，原始文章及附件的哈希保持一致。

未重做Windows原生界面、真实中文IME全过程、第三方补全插件组合、长期高负载或付费搜索验收。此前聊天分支、引用定位、文章Base日期等实机记录见 MACOS-VALIDATION.md。本次自动化回归覆盖聊天、取消、分支历史、记忆权限、目录、来源及补全；不能将自动化通过称为所有原生交互已重新验收。

## 自动化复验

最终0.7.0通过类型检查、构建、159项离线测试及16项运行时集成，无跳过，包含两版旧历史迁移与三文件安装。生产EditorView补全回归和选区/会话组件回归通过。测试后恢复补全关闭并归档临时片段；96个原文章/附件文件哈希一致。三文件安装测试使用合成服务，真实供应商本轮共37次补全请求（16次真实片段批量、20次合成、1次原生副本），不会将其等同于37条高质量候选。
