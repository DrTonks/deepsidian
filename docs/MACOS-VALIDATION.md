# macOS 兼容性验收

## 0.6.3 实现验收（2026-09-20）

- 115 项离线测试、类型检查、构建通过；8 项 DSH 集成通过，无跳过。
- 官方付费 `deepseek-flash` 知识工具场景 3/3，通过报告位于 `.runs/live-knowledge-BWMiTK`；来源组件的定位、断链反馈、候选、附加和跳转检查通过。
- 独立 macOS Obsidian 1.13.7 测试库新建目录后，修改标题/分类并改名文章，差异正确显示新增1、移除1、变化1。确认更新后手写前言/后记保留，自定义 Base 与更新前逐字节一致。
- 原生 `/context` 的“跳转原文”打开正确笔记。真实模型回答引用被点击后，编辑器定位到 `## C#`，没有创建额外笔记。
- 预览后冲突、未保存导航、生成区域被改、重名链接及未保存标题由确定性回归覆盖。仍未声称验证所有主题、最低宿主版本或 Windows 实机。


## 0.6.2 复审（2026-09-20）

本地从已提交的 `7a2db96` 快进到 `b6b00be`，整合两个远程提交。审查新增文章目录、知识库工具、来源预览及设置分组；DSH 保持项目基线 `0.1.6-alpha.2`，七个诊断组件版本一致。

发现并修复：Markdown 标题解析把 `## C#` 末尾的字面井号当作闭合标记移除，导致链接定位报“找不到标题”。现仅移除空白分隔的闭合标记，保留 `C#`、`F#`；新增回归测试在修复前失败、修复后通过。

- `npm ci` 与 `npm run check` 通过：110 项离线测试、类型检查及生产构建。
- DSH 集成 8/8 通过，无跳过，包括 rc.2 旧会话迁移。
- `npm run live:knowledge` 使用真实 `deepseek-official / deepseek-flash`，属性筛选、标题定位、关联证据 3/3 通过。逐例阅读结果符合合成资料；第一例额外输出一句英文开场，不影响结果。
- macOS Obsidian 1.13.7：安装到独立中文空格路径测试库，重载正常；`/context [[posts062/语言验收#C#]]` 正确返回行 9–12，附加后 Cmd+Enter 发送，真实 flash 回答 `MAPLE-062` 和 `7319`。
- `/catalog` 扫描两篇合成文章，预览属性缺失统计正确；确认后生成 Base 与导航，原文未修改。实际打开 Base 显示两篇文章及分类、标签、日期、草稿字段，切换“草稿”视图仅显示一篇草稿。

本轮未重测全部主题、输入法组合、最低 Obsidian 版本或 Windows/Linux 实机；不把已有自动化覆盖视为这些环境的实机通过。付费脚本报告位于忽略提交的 `.runs/live-knowledge-P59fs4/report.json`。

## 0.6.0 复审（2026-09-19）

拉取远程四个提交至 `2e1d502`，保留本地付费测试配置；三个子代理分别审查记忆、运行时与 macOS 界面。本机 DSH 已从 rc.2 升级到项目要求的 `0.1.6-alpha.2`，诊断涉及的七个组件版本一致，旧包单独保留用于迁移验证。

本轮修复提案窗口异步重绘丢失键盘焦点及来源展开状态的问题；记忆工具栏允许换行，限制来源会话下拉宽度并处理提案长文本，避免窄窗口横向溢出。所有付费测试脚本固定使用 `deepseek-official / deepseek-flash`。

验证结果：

- `npm run check`：90 项离线测试通过，类型检查与生产构建通过。
- `npm run test:integration`：8 项通过、无跳过；设置旧包路径后实际覆盖 rc.2 → 0.1.6 Messages 会话迁移，另覆盖取消、图片、记忆权限与网络工具。
- Finder 最小 `PATH=/usr/bin:/bin`：仍发现 Homebrew Node 与 DSH；`dsh --version` 为 `0.1.6-alpha.2`。
- 浏览器真实组件：附件 8 项、记忆控件 9 项及提案焦点/展开回归通过；390px 窗口的记忆窗口无横向溢出。
- 独立中文空格路径测试库安装成功。macOS Obsidian 1.13.7 实机使用真实 flash：中文粘贴内容完整、Cmd+Enter 发送、回答及用量显示、停止请求、重载后对话与停止状态恢复均通过。
- 实机 `/memory` 页面可切换并保存会话读取/贡献权限；已恢复测试前的开启状态。`/extract` 用 Tab/Space/Enter 展开来源并生成，完成后来源仍展开，焦点回到“重新生成提案”，无符合条件的内容时不产生记忆提案。
- 付费脚本共 27 个合成场景通过；24 个记忆案例另有子代理逐例语义复核，见 [真实供应商验证](LIVE-VALIDATION.md)。

边界：中文输入法组合过程、截图粘贴、最低 Obsidian 版本与全新 Mac 首次安装仍未实机复验；Windows/Linux 本轮未实机运行。下文保留旧版本历史记录，不代表本轮结果。

日期：2026-09-14。已整合远程 `c4bc2c2`、`c23db2b`（插件 0.4.1）与本地兼容性修复。以下记录区分初次实机验证和本次复审。

## 修复内容

- 自动发现支持 Apple Silicon `/opt/homebrew`、Intel `/usr/local`、PATH 中的 nvm 安装和自定义 npm prefix；保留 Windows roaming npm 路径。显式路径优先，不启动 shell，也不把 Electron 当作 Node。
- 取消超时显式使用 `node:timers`，避免 renderer 的 DOM timer 没有 `.unref()`。
- 用量独立保存在轨迹结构中，避免长回复的展示快照截断影响统计；兼容旧版有效 JSON。已经截断且未保存结构化用量的旧记录无法据此恢复。
- 侧栏注册 Obsidian `Scope`，避免宿主抢先处理 macOS Cmd+Enter。仅学习问题输入框触发发送，输入法组合期间不发送。
- CI 的基础检查和手动集成测试均配置 Windows、macOS、Linux 矩阵。

## 本地验证结果

环境：Apple Silicon macOS，Node.js 26.6.0，Obsidian 运行版本 1.13.7，全局 DSH 0.1.5-rc.2。CI 使用 Node.js 24。

| 验证 | 结果 |
| --- | --- |
| 类型检查、生产构建 | 通过 |
| 离线测试 | 31 项通过，包含路径发现、DOM timer、长回复用量、宿主持久化、记忆事务与草稿 |
| DSH 集成测试 | 3 项通过，其中准备期取消包含 8 个场景；还覆盖工具、reasoning、图片、参数、重启恢复和网络工具边界 |
| 模拟 Finder 最小 PATH | `PATH=/usr/bin:/bin` 下仍能发现 Homebrew Node 和 DSH |
| 中文及空格路径安装 | 独立测试库安装成功 |
| 浏览器预览构建 | 通过 |

本机全局 DSH 已升级为 0.1.5-rc.2，230 个 DSH 子包均为 rc.2，`dsh --version` 与诊断一致。本次集成测试使用全局安装。2026-09-14 核对 npm 全部发布版本后，最高已发布版本仍是 rc.2（next 标签）；latest 标签仍指向 rc.1，因此使用精确版本。引导、诊断、运行时兼容提示和 CI 统一读取 `src/plugin/versions.ts` 的 `TESTED_DSH`。

## 初次 Obsidian 实机验收（2026-09-13）

使用独立合成笔记库和仅绑定 `127.0.0.1` 的合成模型服务。测试配置与凭证为合成值，运行时未读取个人笔记或发送付费模型请求。

1. 插件加载成功，侧栏及引导正常显示；连接 rc.2 后读取到 4 个候选模型。
2. 从中文 Markdown 笔记发送首轮请求，轨迹中包含正确路径及正文快照；Markdown 回答、笔记链接和 `输入 100 · 输出 20 tokens` 正常显示。
3. Ctrl+Enter 发送持续输出请求，点击停止后显示“已停止”，合成服务确认连接已取消。
4. 重载 Obsidian 测试库后，原对话与停止状态保留。Cmd+Enter 成功发送续聊；合成服务确认请求包含首轮历史。
5. 约 280 像素窄侧栏下检查对话及轨迹页，核心控件可用。

## 整合复审与修复（2026-09-14）

三个子 agent 分别复审运行时、会话与指令、记忆存储。除了保留初次四项 macOS/轨迹修复，本次还修复：

- 会话准备期取消没有生效：立即确认取消，阻止取消请求提交模型，并允许立即重试。确定性测试覆盖模型查询、图片保存、stat、新建、恢复与已有会话等 8 个场景。
- 长问题在 12000 字符处被静默截断：保留完整问题，由整体 40000 字符预算拒绝超限输入。
- 主进程先校验旧上下文、后发送新上下文：先捕获，再校验并复用同一份冻结提示词。
- `/goal` 保存失败仍使用新目标：失败恢复原目标。
- `/new` 未等待持久化且无法反馈失败：新建时阻止并发修改，等待保存，失败恢复原会话；所有入口处理异步结果。
- 记忆管理保存一项或切换标签会丢失其他草稿：保留每项未提交编辑，仅清除已成功提交的内容。

历史搜索真实浏览器组件的 9 项检查通过，覆盖标题/正文匹配、键盘与焦点、busy 状态、异步选择和 dispose。

## 整合版本实机结果（2026-09-14）

在原独立测试库安装 0.4.1 整合构建，使用全局 rc.2 和本地合成供应商：

- 布局恢复后的自动连接通过，Cmd+Enter 发送后显示完整回答与用量；服务端确认未注入旧学习背景。
- `/goal`、`/new` 正常执行；历史搜索输入中文后按 Enter 成功切回原会话。
- `/remember` 保存成功，`/memory` 可看到条目并切换整理规则。
- 草稿、取消及重载的最后一轮 GUI 复查因窗口不再可访问而中断，不计为本轮实机通过。草稿保留有真实组件的离线回归；取消与恢复由本次集成测试覆盖，初次实机结果单列于上文。

## 验证边界

尚未验证真实供应商权限与计费、截图粘贴、所有主题、长历史性能、最低 Obsidian 1.7.2，以及全新 macOS 机器的首次安装。Windows/Linux 的本次代码仅通过离线路径测试覆盖，远端三平台 CI 需在提交推送后查看实际结果。

## 复查命令

```sh
npm ci
npm run check
npm run doctor
DSH_PACKAGE_ROOT="$(npm root -g)/@deepseek-ai/dsh" npm run test:integration
npm run install:dev -- "/path/to/test vault"
```

GUI 合成验收资源保留于被 Git 忽略的 `.runs/`，不作为发布产物。生产安装前，应使用版本一致的 DSH 并配置实际供应商。

## 0.6.4 分支验收（2026-09-20）

- 环境：macOS、Obsidian 1.13.7、隔离库 `.runs/Mac 审查库`，DSH 七组件 0.1.6-alpha.2。已安装本地构建 0.6.4。
- 117 离线测试、8 集成测试通过；类型检查和构建通过。集成包含 rc.2 历史迁移、真实 DSH 分支事件截断、图片/工具历史继承、父子及孙分支隔离、停止子回答与进程重启。
- `npm run live:fork` 使用 `deepseek-official/deepseek-flash` 付费请求，仅合成代号：父 CEDAR→MAPLE，早期分支仍为 CEDAR；子改为 BIRCH 后父仍为 MAPLE；重启子仍为 BIRCH。6/6 通过。报告 `.runs/live-fork-3jo1IX/report.json`，无个人笔记。
- 原生 UI：重载后旧回答的分支按钮禁用并说明原因；新建合成对话返回 CEDAR-064，点击分支后标题/来源显示正常，⌘ Enter 续聊正确返回 CEDAR-064；“返回原对话”只显示原来的一个回答，发送控件可用。约 300px 宽侧栏中回答下分支图标无溢出。
- 最终构建再次重载后，历史列表保留父会话 2 条消息和分支 4 条消息；从历史切回分支，来源关系、返回入口及再次分支按钮均恢复可用。
- 保留边界：Windows 原生 UI 未在本轮实机验证；首版不合并分支、无聊天树；共享笔记及长期记忆不属于分支隔离范围。插件存储失败会保留父会话，但可能留下孤立 DSH 日志，不自动清理。

### ab3b493 拉取复审

已 fetch 并确认本地 main 与 origin/main 同为 ab3b493；开始审查时工作区干净。重点复审目录写入回调竞态及 CRLF 兼容修复。

发现并修复 CRLF 编辑器比较遗漏：磁盘为 CRLF，CodeMirror 文档输出为 LF 时，未改动的导航会被误判为存在未保存编辑。新增生产 CatalogModal＋磁盘 Vault＋实际 CodeMirror 文档回归，先复现失败，再仅对编辑器内容比较归一化 CRLF；磁盘预览快照比较仍严格匹配，输出换行仍保留原格式，实际未保存文字仍拒绝覆盖。

最终类型检查、120 离线测试及构建通过，8 DSH 集成测试通过（集成在本次目录比较修复前运行，修复不涉及运行时）。本次没有重新执行付费模型或原生 Obsidian UI 验收。修复与回归保留本地，未提交或推送。


## 0.6.5 最终范围验收（2026-09-21）

按用户决定删除结论回传，仅保留独立分支、上下文清单和会话文字草稿。最终122项离线测试、类型检查及构建通过。新增回归验证图片/文本来源清单、哈希变化、会话草稿隔离与持久化，以及旧原型残留字段不会进入新模型请求。

已更新 `.runs/Mac 审查库` 并在 Obsidian 1.13.7 重载：历史列表可以进入分支，保留“返回原对话”和“分支到新聊天”，无“带回父对话”入口；上下文清单入口存在。此前原型的回传与付费测试属于历史试验，不代表最终产品包含回传。未删除试验聊天和运行时历史；本轮未重新执行付费模型测试。Windows 原生界面未实测。

最终 DSH 集成8/8通过（含旧版会话迁移）；上下文弹窗在原生界面正确显示4条历史、2条继承消息及记忆状态。git diff --check通过，代码未提交或推送。


### 0.6.5 子 agent 复审（2026-09-21）

三位子 agent 分别审查草稿生命周期、上下文清单及界面交互，发现两项 P2，均已修复：库内附件异步读取期间切换会话可能将附件加入新会话，现固定来源会话并在读取前后检查；当前笔记移除操作实际持续生效，现明确提示并提供手动重新附带入口。系统文件选择器也在打开时固定来源会话。

修复后 npm run check 通过：122 项离线测试、类型检查与构建。真实 LearningView 浏览器组件回归通过，覆盖草稿恢复与会话隔离、附件移除、延迟库内附件读取时切换会话、正常附件添加、笔记持续排除与手动恢复，以及无回传入口。DSH 集成 8/8 沿用此次界面修复前的最终范围验收；本次未新增付费模型请求。原生 macOS 验收沿用上节记录，不将浏览器组件测试计为原生实测。代码保留本地，未提交或推送。


## 手动 Tab 补全实验（2026-09-21）

Obsidian 1.13.7 隔离库实机确认：手动命令生成付费 flash 灰字，显示时磁盘正文未变；Tab 接受、⌘Z 单次撤销正常。稳定0.6.5回退后补全命令消失，5个会话及草稿保持；随后恢复实验构建。133项离线与12项DSH集成通过。中文IME本轮未能进入真实组词态，仅浏览器合成事件覆盖，不计原生IME通过；Windows原生待验收。详细付费质量限制和范围见 [实验记录](TAB-COMPLETION-EXPERIMENT.md)。

## 0.6.6 远程主分支复审（2026-09-22）

工作区开始时干净，位于 experimental-tab-completion 分支；保留该分支，切回 main 并从 001d1de 快进至 origin/main 的 2e557c7。三位子 agent 分别审查选区生命周期、来源定位及侧栏交互，修复三项问题：

- 会话切换保存期间未锁定宿主，右键选区提问可能将原会话草稿写入目标会话；切换现在同步加锁，成功和失败均释放。
- 编辑器 LF 与磁盘 CRLF 导致来源版本假变更，丢失原行号；来源修订统一逻辑换行并兼容旧版原始哈希，实际正文或行数变化仍拒绝沿用旧行号。上下文清单的精确内容指纹不变。
- 请求接受后草稿选区已清空，但无 Markdown 编辑器时来源仍保留旧快照；现在清除已消费来源并重新捕获可用编辑器，本轮请求及历史记录保留原快照。

修复后 npm run check 通过：138 项离线测试、类型检查与构建。新增回归覆盖延迟保存的切换成功/失败、关闭来源后的连续发送、新旧来源修订与 CRLF 定位；会话竞态与重复快照均先复现失败再修复。生产侧栏 learning-tests 浏览器回归通过。DSH 8 项集成通过（此次宿主修复前运行，未改运行时桥接）。另一位子 agent 独立复核全部修复，未发现新增确定问题。

官方 deepseek-official/deepseek-flash 付费合成验收 3/3，通过充分选区零工具回答、related→resolve 小节取证、资料中的记忆指令不执行；已人工检查完整回答和工具记录。报告保留于 .runs/live-selection-YDMop8/report.json，使用 DSH 七组件 0.1.6-alpha.2。

最终构建安装至隔离的 .runs/Mac 审查库，在原生 Obsidian 1.13.7 重载。实测右键“向 Deepsidian 提问”保留已有草稿，预览显示标题“缓存边界”和行 4；重载后草稿与快照恢复。关闭来源笔记后发送一次合成问题，官方 deepseek-flash 正确返回阈值 7319 和标记 CEDAR-MAC-066；下一轮清单不再附带已消费快照。未使用个人笔记作为模型输入。Windows 原生交互本轮未实测。

修复及记录留在 main 工作区，未提交或推送；Tab 补全实验分支未合并。

## DSH 0.1.7-rc.2 兼容验收（2026-09-26）

本机全局 DSH 从 0.1.6-alpha.2 升级到 0.1.7-rc.2，doctor 确认主包与六个受检组件均为 rc.2。该版本属于 npm next；本日 latest 仍为 0.1.5-rc.3，README 使用精确版本安装。旧运行时保留于忽略的 .runs/dsh-016-baseline，供迁移验证。

适配上游移除 settings-file 和官方 Chat Completions 协议：显式导入允许的供应商配置，按旧 settings、SDK profile patch、公共 patch 的顺序应用；保留最终禁用状态及 config 整体替换语义。只读取普通 YAML，不导入任意工具插件；Web/Desktop 专属 profile 配置不自动继承。移除的 protocol 字段明确报迁移错误。旧运行时继续使用旧组合，模型预览与实际启动读取相同来源。另修复 knowledge.ts 的 Node 原生 TypeScript 导入扩展名，并让离线测试直接导入生产模块。

两位子 agent 审查运行时配置及测试，修复禁用状态过早判定、旧运行时模型预览与启动配置不一致，以及删除记忆断言可能被历史内容误满足的问题；独立最终复核未发现新增确定问题。

最终验证：

- npm run check：139/139 离线测试、类型检查及构建通过；git diff --check 通过。
- 全局 rc.2 的 DSH 集成 11/11，无跳过。包含 0.1.5-rc.2 与 0.1.6-alpha.2 历史迁移、分支边界和重启、Messages 流式与工具/图片、取消、记忆权限，以及合成 HTTP 自定义供应商和网页工具。配置测试包含禁用后恢复、无关适配器禁用和空配置替换。
- 官方付费 deepseek-official/deepseek-flash：选区 3/3、分支 6/6、记忆管理 8/8；最终修复后使用全局安装重跑选区 3/3，并检查完整回答及工具记录。输入均为合成资料，无个人笔记。报告分别为 .runs/live-selection-iOlwlC/report.json、.runs/live-fork-Xgamwn/report.json、.runs/live-manage-7YXE7C/report.json、.runs/live-selection-MCwKR7/report.json。自动报告的 review-pending 字段保留原始结果；人工复核记录在本文，不将自动断言当成全面语义质量保证。
- 最终构建安装至隔离 .runs/Mac 审查库，在原生 Obsidian 1.13.7 重载。状态显示 DSH 0.1.7-rc.2，无“此版本未验证”；升级前的合成会话继续调用官方 deepseek-flash，约 2 秒完成，准确返回阈值 7319、标记 CEDAR-MAC-066。输入 2436、输出 102 tokens，缓存读取 74%；输入区和分支按钮恢复可用。

本轮未验证 Windows 原生界面、全新机器安装或独立 DSH 终端的原生 shell 工具。新版 DSH 桌面端能力不等于已集成至 sdk-minimal 插件。代码与记录留在本地，未提交或推送。


## Tab 与 0.6.6 / DSH 0.1.7 整合（2026-09-26）

已整合 main `76f8da4` 与手动 Tab 实验，保留默认关闭。三位子代理审查，修复选区提问取消旧补全的时机并迁移 Messages-only 集成测试。156 项离线、15 项本地 DSH 集成（含两个历史版本迁移）、补全与学习组件浏览器回归通过。原生 macOS 1.13.7 隔离库实测付费灰字、Tab 接受、单次撤销及选区快照入口共存。真实边界样例仍有已知质量失败；详见 [Tab 实验记录](TAB-COMPLETION-EXPERIMENT.md)。本次整合不新增自动触发或稳定版发布标签。

## PR #1 合并后与社区发布准备（2026-09-26）

fetch 后确认本地和 origin/main 同为 30a42b1，PR #1 已合并，本轮没有再次合并、提交或推送。三位子 agent 分别复核补全、现有功能回归和社区发布；修复后另外独立复核发布桥接及日期兼容。

发现并修复两项实际问题：

- 社区标准安装只有 main.js、manifest.json、styles.css，旧构建依赖额外 bridge.mjs。现将桥接源码及第三方 MIT 通知静态纳入 main.js，在插件 .runtime 内按内容哈希原子生成运行文件，普通聊天和隔离整理共用。测试仅安装三个文件，实际执行对话、重连/损坏修复和无工具整理。
- 真实 Astro 文章的 published 字段未纳入目录日期规则，27 篇均被误报缺日期。现按 published → date → pubDate 兼容，预览、缺失统计和生成 Base 公式一致，原文章保持不变。

最终 npm run check：158/158、类型检查与构建通过。发布桥接版 DSH 集成 16/16，无跳过，包含两个旧运行时迁移和新三文件安装；后续日期修复不涉及运行时，增加目录回归并通过全量 check。真实 EditorView 浏览器补全回归通过，覆盖灰字、Tab/Escape、撤销/重做、迟到结果、文件切换、组词事件、失焦及销毁；不等于真实中文输入法验收。

### 用户指定 content 工作区实测

工作区：/Users/tonks/myserver/blogExample/src/content，Obsidian 1.13.7，DSH 0.1.7-rc.2。本轮开始该库没有 Deepsidian，只有一个未启用的博客工具。备份 .obsidian 顶层 JSON，并记录 103 个原内容文件的哈希。只复制三个社区安装文件后，在原生界面刷新插件并启用。

已通过：

- 首次加载与自动连接：显示官方 flash 和 rc.2，无额外 bridge.mjs 依赖。
- 上下文清单与选区提问：保留已有问题草稿，完整合成选区为第 1–16 行；实际回答约 2 秒，准确给出 7319 / CEDAR-REALVAULT-0926。当前笔记只附带光标附近内容，清单可发现未选择到目标段落的情况。
- 分支：子会话精确继承首轮；续聊实际调用一次 obsidian_resolve，读取指定合成关联小节，准确返回 8246 / MAPLE-REALVAULT-0926。点击引用跳到正确笔记与标题。重载后保留父 2 条、子 4 条消息，返回父会话未混入子回答。
- 手动补全：原生灰字给出“12 毫秒。”；Tab 接受，一次 Cmd+Z 仅撤销该候选。持久统计为调用/展示/接受各 1。测试后关闭实验开关。
- 真实文章目录：首次暴露 published 误报；修复后重新扫描真实 posts 的 27 篇，所有列缺失计数为 0。在专用测试目录确认生成 Base 和导航，原生 Base 显示 27 个结果和正确发布日期，未修改文章属性。

真实模型请求只使用新建的两篇合成资料，没有发送已有文章。允许发送哪些现有文章的范围尚待用户确认。现有文章的分类/日期只做本地预览及 Base 验证。本轮没有在该真实库重新执行记忆写入、付费网页搜索、长历史性能、真实中文 IME 或启用其他第三方补全插件的兼容验收，不能据此宣称全部功能已全面验收。

测试结束将合成笔记和生成管理文件移出博客内容目录，保留于忽略目录 .runs/pr1-real-vault/archived-test-notes；103 个原文件哈希全部一致，额外内容文件数为 0，博客 Git 工作区保持干净。插件保持安装启用，实验补全已关闭，合成聊天及运行日志保留（原合成来源已归档，历史链接不再指向现存库内文件）。备份及最终哈希报告在 .runs/pr1-real-vault。

### 付费补全质量复核

官方 deepseek-flash 留出集 8 次：6 候选、2 空返回，0 错误、0 超时，全部报告用量；返回延迟中位数 640ms、P95 787ms，不含冷启动。人工复核为 5 条可用、2 条合理拒答、1 条语义不合格：错误“栈是先进先出”前提被接成“最早入栈最后出栈”，整句自相矛盾。报告 .runs/live-completion-6fgWIO/report.json，人工结论 manual-review.json。此项保留失败，不通过反复重试隐藏。实验继续默认关闭，不应宣传成可靠事实校验或自动写作。

### 上架尚未完成

官方命名规则禁止 -sidian 变体，当前 Deepsidian 显示名称须先由用户决定替代名；仓库及数据 ID 可先保留。最低版本已收敛到实测 1.13.7，补充费用/网络/库外读取披露、版本映射及三文件打包命令。当前仓库尚无 GitHub Release，也未在 community.obsidian.md 提交审核。步骤见 COMMUNITY-RELEASE.md，不将本地准备等同于社区审核通过。

### 显示名称更新（2026-09-26）

用户确定新名 Deepseedian，覆盖上一节的待定名称状态。manifest、侧栏/设置/选区菜单等显示文案、安装说明及三文件产物已更新；插件 ID、数据目录、协议和已有存储标记保留 deepsidian。158 项离线、类型检查和构建通过。本轮未发起任何付费请求。已更新 content 库内三个插件文件，旧构建备份在 .runs/deepseedian-rename-backup；重载后显示新名称，聊天及配置保留。社区名称唯一性及审核仍以正式提交结果为准。

进一步澄清：社区发布不需要上传个人文章；只有使用云端 AI 功能时才会发送所需上下文，发布回归可全部使用合成资料。本地目录/Base操作不调用模型。已有文章外发范围仍未获确认，本轮未发送。

### 0.6.6 发行复验（2026-09-26）

维护者批准提交、推送和创建公开 GitHub Release，执行范围停在社区提交之前。最终代码重新执行 npm ci、npm run check 与运行时集成：158 项离线、16 项集成全部通过，无跳过；集成包含两个历史版本迁移。doctor 确认七个运行时组件均为 0.1.7-rc.2，标准三个发行文件重新打包。本轮未增加付费模型请求。此前“尚无 Release”“未提交或推送”等表述仅记录当时状态；最新发行状态见 GitHub Releases，社区审核尚未发起。
