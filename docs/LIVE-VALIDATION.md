# 真实供应商验证

## 0.6.0 / DSH 0.1.6-alpha.2（2026-09-19）

本轮四个付费脚本全部固定为 `deepseek-official / deepseek-flash`，使用本机 DSH 凭据及合成资料。共 27 个脚本场景自动检查通过：

| 命令 | 场景 | 结果 |
| --- | --- | --- |
| `npm run live` | 工具与流式聊天、偏好提炼、虚构人物排除 | 3/3 |
| `npm run live:memory` | 保存后读取、更正、进程重启、删除后旧会话撤回 | 4/4 |
| `npm run live:manage` | 关闭权限、自然语言新增/读取/更正/删除、虚构/临时请求排除、重连后禁用 | 8/8 |
| `npm run eval:memory` | 归属、临时请求、注入、凭据、复合更正、遗忘等固定评测 | 12/12 |

子代理逐例阅读了后面三组 24 个案例的输出、工具结果和记忆快照，记忆语义通过；评测报告已补充 `semanticReview`，明确为子代理复核，非用户人工验收。提炼正例引用支持偏好，负例返回空提案。

保留的质量问题：重启读取案例额外展示了内部记忆 UUID；小说润色有超出“意思不变”的创造性扩展；聊天样例对 KV cache 计算复杂度的说明过于简化，忽略了新 token 仍需关注历史缓存。自动链路通过不代表知识解释均准确。

报告目录均位于不提交的 `.runs/`：`live-2026-09-19T13-04-06.618Z`、`live-memory-2026-09-19T13-04-49.770Z`、`live-manage-5PqXkN`、`memory-eval-Wwplxu`。另在独立 Obsidian 测试库验证真实聊天、取消和提炼，见 [macOS 验证](MACOS-VALIDATION.md)。未查询供应商账单，不推算实际金额。

## 首次真实请求（升级前，同日）

2026-09-19 执行 `npm run live`，使用本机 DSH 凭据调用
`deepseek-official / deepseek-flash`，DSH 组件版本为 `0.1.5-rc.2`。
测试资料为合成内容，不读取个人笔记。

- 结果：`completed`，耗时 6 秒，2 次工具调用，331 个文本流片段。
- 两步模型响应报告的 `totalTokens` 合计为 2650，其中缓存读取 1280，输出 865。
- 实际金额以供应商账单为准；测试未查询账单。
- `npm run typecheck` 通过。

人工检查：回答涵盖 KV cache 的用途及显存代价，但除三句话解释外还附加了开场、示例和追问，没有严格遵守长度要求。
工具处理器为合成实现；本次证明真实供应商经过 DSH 和插件桥接完成了工具与流式链路，不代表完整 Obsidian 界面体验已重新验证。

后续涉及模型、运行时、工具或提示词的开发，使用已授权的此付费模型做真实验证，并检查内容质量。默认离线测试和 CI 不自动产生费用。
