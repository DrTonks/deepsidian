# Deepseedian 0.7.1 审核修复验证

日期：2026-09-26。针对 0.7.0 社区扫描的阻断错误修复，不覆盖旧版本。

## 修复范围

- 目录预览的 max-height、overflow、white-space 移入限定在目录弹窗内的 CSS 类，保留 260px 高度限制和换行行为。
- 设置页使用 Setting.setHeading，并将冗余插件名称标题改为“使用前须知”。预览宿主同步支持标准标题。
- 接入官方 eslint-plugin-obsidianmd 0.4.2，npm run check 和三平台 CI 均执行预检。Obsidian 规则保留推荐严重程度；既有通用 JS/TS 问题作为可见警告逐步治理，不宣称全部清零或替代社区扫描。
- TypeScript 固定为提供检查器所需编译器 API 的 5.9.3，并显式包含 DOM.Iterable；此前 TypeScript 7 的原生实现无法运行当前检查器。
- 包含先前已提交的英文安装、费用和隐私说明。

## 本地验证

- 从锁文件干净 npm ci，类型检查、构建和 159 项离线测试通过。
- 16 项运行时集成通过，无跳过；包含两版历史迁移和仅三个发行文件的首次安装。
- 官方规则对旧 catalog.ts 检出三处静态样式错误、旧 settings.ts 检出一处手动 HTML 标题错误；修复后 0 个阻断错误，532 个警告仍可见。
- 用户真实库安装三个 0.7.1 发行文件并重新启用插件，macOS Obsidian 1.13.7 确认版本、标准“使用前须知”标题与设置分组正常。
- 真实库 posts 扫描 27 篇文章，缺失属性均为 0，展开 Base 预览正常；只读预览后关闭，未确认创建输出文件。

上述审核修复阶段未修改模型调用或补全逻辑，没有新增付费请求；下文的发布后验收另行执行了真实供应商请求。更广泛的真实文章与补全失败样例见 [0.7.0 验证记录](VALIDATION-0.7.0.md)，不将其称为本补丁新增实测。Windows/Linux 原生界面未重新手动验收。社区审核结论以管理页对新提交/发行版本的实际扫描为准。

## 社区结果

2026-09-26，提交 1582b0c 的分支预审及 0.7.1 正式扫描均为 Completed，无阻断错误。官方构建验证逐字节复现 main.js，依赖检查与混淆检查通过。已发布社区条目，并在公开页面确认版本 0.7.1 与 Add to Obsidian 入口；评分卡仍为 Review: Caution，保留类型、样式、文件系统及进程权限等警告。此记录没有将入口可见等同于已重做社区市场全新安装验收。


## 发布后独立新库验收

2026-09-26，macOS Obsidian 1.13.7、Node.js 26.6.0、DSH 0.1.7-rc.2。此轮不修改插件实现或版本号。

### 社区安装入口：仍未通过

- 新建无第三方插件的测试库，启用社区插件后搜索 Deepseedian 返回 0 个结果；重载后结果相同。
- 社区公开网页的 Add to Obsidian 链接只打开客户端市场，未进入本插件详情或安装流程。
- 19:37（UTC+8）复查官方公开 community-plugins.json，8080 条记录中仍没有 deepsidian/deepseedian。
- 因此仅确认网页公开，不能确认客户端市场安装通过。可能涉及目录同步或缓存，但原因与恢复时间尚未确定；不通过修改插件版本号或手工提交旧式目录 PR 来猜测修复。
- 该库继续保持未安装本插件，便于后续重试真实社区安装。

### GitHub Release 三文件安装：通过

在另一个独立新库，从公开 0.7.1 Release 下载三个附件，没有复制既有 data.json、bridge、运行时或记忆。逐一核对下载文件与正式发行构建的 SHA-256 后，通过 Obsidian 设置启用。

| 文件 | SHA-256 |
| --- | --- |
| main.js | 9ffc1925276f54ac927eb05e2b07dcd4e1c45354738da853b860e414556d41a5 |
| manifest.json | bbd8e1d7268b1ac7bc804a1d31e2fed5cdc415de71208cb065380fed19df1579 |
| styles.css | cffb60d0b40675b4a7754659458a4beec8f6ce065977fbb898054a7204c1ccae |

- 首次启动自动生成内嵌桥接运行文件，发现本机 DSH 与两个模型候选；连接引导成功。凭据仍读取已有 DSH 配置，没有复制进测试库。
- 新安装手动补全为关闭状态；记忆读取、AI 记忆管理、网页搜索与读取为默认开启。实测对话前关闭该会话记忆读取。
- 三次真实 deepseek-official / deepseek-flash 提问：合成笔记信息提取、学习行动卡、只读笔记工具与引用。工具场景可能包含多次底层模型调用，不把三轮提问等同于三次 API 请求。
- 信息提取准确包含 CEDAR-COMMUNITY-071、7319、前缀完全一致及缓存年龄小于 12 分钟；用量为输入 2030、输出 117 tokens。
- 行动卡基于显式选中的演示笔记生成目标、三步和自检问题；用量为输入 2300、输出 249 tokens。界面与保存的轨迹均没有工具调用事件。
- 第三轮轨迹确认 obsidian_read 读取演示/学习复盘.md，工具结果为该合成笔记的带行号原文，最终回答附上真实路径引用。没有将模型声称“调用了工具”当作执行证据。
- 从完成回答创建分支后，实际重载 Obsidian，分支标题、来源、两条继承消息与返回父会话入口恢复正常。
- 本地扫描 notes 中 1 篇文章，缺失属性均为 0；确认后生成文章管理.base 与文章导航.md。
- 原始验收笔记操作前后 SHA-256 相同：41d66090095329a156bdad97240b261a535b3fdccb70719eecf11013a5afaceb。

本轮只向供应商发送合成演示资料，未改动用户原博客文章。未新增补全、付费搜索、Windows/Linux 原生 UI 或全新机器上的环境安装测试。手动 Release 安装通过不能替代社区安装入口验收。

截图与可复现步骤见[功能演示](DEMO.zh-CN.md)。运行日志、会话数据与机器路径保留在本地忽略目录；公开文档仅包含专用演示笔记、截图和脱敏结论。
